TASTY_BYTES_DBT_DB.DEV.TASTY_BYTES_DBT_PROJECT

WITH stg_country AS (
SELECT
*
FROM
TASTY_BYTES_DBT_DB.RAW.COUNTRY
)
SELECT
COUNTRY_ID,
COUNTRY as COUNTRY_NAME,
ISO_CURRENCY,
ISO_COUNTRY,
CITY_ID,
CITY AS CITY_NAME,
CITY_POPULATION
FROM 
stg_country